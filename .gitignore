# Stuff that pops up locally for me - @andrezrv
*.sublime*
.DS_Store

# No need to share our static files
/app/shared/*

# No need to share our local config file
/app/local-config.php

# And no need to share individual SQL files with each other
*.sql
*.sql.gz

# Avoid the automatically generated upgrade folder to reach the repo
/app/content/upgrade/

# No need to share custom local backups
/backups/*

# This is gonna have sensitive information about the database setup
/tasks/config.sh
